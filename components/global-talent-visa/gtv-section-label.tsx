export function GTVSectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <h3 className="mb-4 font-mono text-[0.62rem] tracking-[0.2em] text-accent uppercase">
      {children}
    </h3>
  );
}
